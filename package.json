{
  "dependencies": {
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.33.0"
  },
  "scripts": {
    "netlify:build": "npm install --prefix client && npm run build --prefix client",
    "netlify:deploy": "netlify deploy --prod --dir client/build --message 'Magic Library PWA'"
  }
}
